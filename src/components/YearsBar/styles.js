import styled from "styled-components";

import { colors } from "../../core";

const { whiteDark } = colors;

export const YearsBarContainer = styled.div`
  height: 65px;
  width: 100%;
  background: ${whiteDark};
  display: flex;
  justify-content: center;
  align-items: center;
`;